#include <vector>

// You can use typedef or using to create an alias
// for a given type. In modern C++, using is preferred as it's slightly cleaner.

typedef int IntegerAlias; 
typedef std::vector<int> VectorAlias;

using IntegerAliasUsing = int;
using VectorAliasUsing = std::vector<int>;
